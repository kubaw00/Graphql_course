type Query {
  job: [Job!]
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  title: String!
  description: String!
  """
  Comment date is ISOString
  """
  date: String!
  company: Company
  id: ID!
}
