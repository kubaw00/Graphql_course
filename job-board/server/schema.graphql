type Query {
  job(id: ID!): Job
  jobs: [Job!]
  company(id: ID!): Company
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  title: String!
  description: String!
  """
  Comment date is ISOString
  """
  date: String!
  company: Company
  id: ID!
}
